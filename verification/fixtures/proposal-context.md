# DAO Context Verifier Fixture

This immutable fixture is the canonical context snapshot for lifecycle verification.

The proposal author commits this publication before voting. Validators fetch the same
publication from IPFS and classify only semantic changes before execution.

Expected decision: UNCHANGED when this exact document is retrieved.
